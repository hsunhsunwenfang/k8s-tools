apiVersion: v1
kind: Pod
metadata:
  labels:
    run: ghcr-test
  name: ghcr-test
  namespace: default
spec:
  containers:
  - name: ghcr-image
    image: ghcr.io/hsunhsunwenfang/debug:v1
  imagePullSecrets:
  - name: hsunhsunwenfang-ghcr-secret
  securityContext:
    runAsUser: 1000
    runAsGroup: 3000
    fsGroup: 2000
